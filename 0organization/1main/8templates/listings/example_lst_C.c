void* ¢\color{black}\bfseries some\_function¢(void* args, int argc){
	if(var->entry){
		char* some_point = NULL;
		int len = ¢\color{eclipse_keyword2}make\_something¢($some_point);
		struct timespec tim;
		tim->tv_nsec=200;
		struct some_struct struc;
		struc->member=2;
		while(current != var->iterator){
			//Some Comment
		}
		free(some_point);
	}else{
		switch(¢\color{eclipse_keyword2}get\_type¢(some_point)){
		case val1:{
			¢\color{eclipse_keyword2}another\_func¢(input);
			break;
		}
		case val2:{
			//Do something else
			break;
		}
		default:{
			break;
		}
		}//end switch (type)
	}
	return;
}
